---
const response = await fetch("https://jsonplaceholder.typicode.com/posts");
const posts = await response.json();
---

<h1>mis publicaciones</h1>
{
  posts.map((post) => (
    <div>
      <h3>{post.userId}</h3>
      <h1 class="font-titulos uppercase font-bold text-titulo-600">{post.title}</h1>
      <p>{post.body}</p>
    </div>
  ))
}
